# Live-run evidence (redacted)

Inspectable samples from operator curtain-up runs against CALL-E.
Phone numbers are never stored here. Call run IDs are truncated.

These are **operator-proven** outcomes for the two primary cues — not a claim that every clone will auto-ring without keys.

| Cue | Line reading | Decision |
| --- | --- | --- |
| `stuck_support` | **1** | Take over in chat now |
| `agent_needs_decision` | **1** | Approve A — allow the one-time exception |

Files:

- `prompt-book-sample.ndjson` — two successful curtain-up rows (redacted)
- `show-report-excerpt.md` — matching show-report blocks

Full local audit lives under `skills/customer-success-voice-signal/data/` (gitignored runtime noise may accumulate; do not commit secrets).
