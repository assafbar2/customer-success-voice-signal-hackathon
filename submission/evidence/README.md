# Live-run evidence (redacted)

Inspectable samples from operator curtain-up runs against CALL-E.
Phone numbers are never stored here. Call run IDs are truncated.

These are **operator-proven** outcomes — not a claim that every clone will auto-ring without keys.
See the **full live ladder** (including voicemail and unclear captures) in
[`research/calle-api-notes.md`](../../research/calle-api-notes.md).

| Cue | Best line reading | Decision | Also observed |
| --- | --- | --- | --- |
| `stuck_support` | **1** | Take over in chat now | Prior voicemail → `no_answer` |
| `agent_needs_decision` | **1** | Approve A — allow the one-time exception | Prior `unclear` captures |

Files:

- `prompt-book-sample.ndjson` — successful curtain-up rows (redacted)
- `show-report-excerpt.md` — matching show-report blocks
