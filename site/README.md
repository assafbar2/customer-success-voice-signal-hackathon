# Judge site

Static landing page. GitHub Pages deploys `site/` from `main`.

**https://assafbar2.github.io/customer-success-voice-signal-hackathon/**

Local: `cd site && python3 -m http.server 4173`

Social preview image: `og.png` (1280×640). Pages already serves it. The **GitHub repo card** still needs a manual upload: Settings → General → Social preview ← `og.png` (no working API for this token).
