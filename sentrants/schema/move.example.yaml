# Something we did. Hits a portfolio id, not a brand sticker.

id: seer_auto_opens_prs
kind: ship # ask | tell | price | ship | break | take
target: seer.auto
remember: false

hat: product

what:
  title: "Seer will open PRs on issues it thinks it can fix"
  body: |
    No extra click. Autofix → PR. Off unless they turn it on? We're deciding.

who:
  exclude_stage: [gone]

camps:
  - id: yes_leave_it
    label: Leave it on
  - id: yes_but_manual
    label: Fine if I click it
  - id: hell_no
    label: Not in my repo
  - id: not_me
    label: I don't use Seer
  - id: leaving
    label: I'm done

# Change target or what.body — whole swarm walks again.
